export interface Post {
  title: string;
  date: string;
  description: string;
}

export interface Project {
  imageUrl: string;
  title: string;
  description: string;
}

export interface SectionHead {
  title: string;
  linkTitle: string;
  linkAddress: string;
}
