export interface Post {
  title: string;
  date: string;
  description: string;
  slug: string;
}

export interface Project {
  imageUrl: string;
  title: string;
  description: string;
  slug: string;
}

export interface SectionHead {
  title: string;
  linkTitle: string;
  linkAddress: string;
}
