export interface MyPost {
  title: string;
  date: string;
  description: string;
}

export interface MyProject {
  title: string;
  date: string;
}

export interface SectionHead {
  title: string;
  linkTitle: string;
  linkAddress: string;
}
