---
import ArticleList from '@components/Posts/PostsList.astro';
import Layout from '@layouts/BaseLayout.astro';
import { getAllPosts } from '@utils/content';

const posts = await getAllPosts();
---

<Layout>
  <ArticleList posts={posts} />
</Layout>
