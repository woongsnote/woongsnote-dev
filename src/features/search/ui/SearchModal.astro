---
const { id = 'search-modal' } = Astro.props;
---

<div
  id={id}
  class="fixed inset-0 z-50 hidden"
  aria-hidden="true"
  data-search-modal
>
  <div class="absolute inset-0 bg-black/40" data-close></div>

  <div
    class="bg-background relative mx-auto mt-24 w-[min(720px,92vw)] rounded-2xl shadow-xl"
  >
    <div class="border-b p-3">
      <input
        type="search"
        placeholder="검색… (⌘K / Ctrl+K)"
        class="w-full rounded-lg border px-3 py-2 outline-none"
        data-search-input
      />
    </div>

    <div class="max-h-[60vh] overflow-auto p-3">
      <ul class="space-y-2" data-results></ul>
      <p class="text-sub mt-3 text-sm" data-empty hidden>검색 결과가 없어요.</p>
    </div>

    <div
      class="text-sub flex items-center justify-between border-t p-3 text-xs"
    >
      <span>ESC 닫기</span>
      <span>⌘K / Ctrl+K 열기</span>
    </div>
  </div>
</div>

<script>
  import './SearchModal.client';
</script>
