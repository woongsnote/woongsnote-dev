---
type Props = {
  tag: string;
};
const { tag } = Astro.props;
---

<span
  class="rounded-full border border-black px-2 py-0.5 text-xs text-content dark:border-gray-50 sm:text-sm"
  >{tag}</span
>
