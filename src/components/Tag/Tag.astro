---
type Props = {
  tag: string;
};
const { tag } = Astro.props;
---

<span
  class="text-primary inline-flex items-center rounded-md bg-gray-50 px-2 py-1 text-xs font-medium ring-1 ring-gray-500/10 ring-inset dark:bg-gray-700"
  >#{tag}</span
>
