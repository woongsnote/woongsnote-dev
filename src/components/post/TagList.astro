---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class="flex flex-wrap items-center gap-2">
  {
    tags.map((tag) => (
      // ✅ 수정
      <span class="tag-chip">#{tag}</span>
    ))
  }
</div>
