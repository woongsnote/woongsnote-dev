---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <ul aria-label="태그" class="flex flex-wrap items-center gap-1.5">
      {tags.map((tag) => (
        <li>
          <span class="tag-chip">#{tag}</span>
        </li>
      ))}
    </ul>
  )
}
