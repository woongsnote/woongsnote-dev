---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class="flex flex-wrap items-center gap-2">
  {
    tags.map((tag) => (
      <span class="bg-secondary font-medium inline-flex items-center rounded-md px-2 py-1 text-xs">
        {tag}
      </span>
    ))
  }
</div>
