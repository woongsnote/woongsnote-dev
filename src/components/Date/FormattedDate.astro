---
import { format } from 'date-fns';
type Props = {
  date: Date;
};

const { date } = Astro.props;

const displayDate = format(date, 'yyyy년 MM월 dd일');
const isoDate = format(date, 'yyyy-MM-dd');
---

<time datetime={isoDate} class="whitespace-nowrap text-sm">{displayDate}</time>
