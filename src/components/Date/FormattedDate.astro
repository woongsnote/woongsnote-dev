---
import { format } from 'date-fns';
type Props = {
  date: Date;
};

const { date } = Astro.props;

const displayDate = format(date, 'yyyy년 M월 d일');
const isoDate = format(date, 'yyyy-MM-dd');
---

<time datetime={isoDate} class="whitespace-nowrap">{displayDate}</time>
