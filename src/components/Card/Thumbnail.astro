---
import { Image } from 'astro:assets';

type Props = {
  title: string;
  image: string | undefined;
};

const { title, image } = Astro.props;
---

<div class="hidden overflow-hidden rounded-lg md:block">
  {
    image && (
      <figure class="transiton w-40 ">
        <Image
          src={image}
          alt={title}
          width={320}
          height={180}
          class="rounded-lg object-cover object-center duration-300 ease-in-out hover:scale-110"
          loading="eager"
        />
      </figure>
    )
  }<span class="sr-only">Go to {title} page</span>
</div>
