---
import CardImage from './CardImage.astro';

type Props = {
  title: string;
  image: string;
};

const { title, image } = Astro.props;
---

<Fragment>
  <CardImage title={title} image={image} />
</Fragment>
