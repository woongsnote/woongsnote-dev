---
import { SOCIALS } from '@config/site-config';
import SocialLink from './SocialLink.astro';
---

<nav class="grid grid-flow-col gap-2 items-center justify-center">
  {SOCIALS.map(({ name, url }) => <SocialLink name={name} url={url} />)}
</nav>
