---
import { SOCIALS } from '@config/site-config';
import SocialLink from './SocialLink.astro';
---

<div class="flex gap-4">
  {
    SOCIALS.map(({ name, svg, url }) => (
      <SocialLink name={name} svg={svg} url={url} />
    ))
  }
</div>
