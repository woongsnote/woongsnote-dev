---
type Props = {
  category: string;
};
const { category } = Astro.props;
---

<span
  class:list={[
    'rounded-md px-2 py-1 text-xs font-black text-white lg:text-sm',
    category === 'Project'
      ? 'bg-blue-500'
      : category === 'Diary'
        ? 'bg-green-500'
        : 'bg-gray-500',
  ]}
>
  {category}
</span>
