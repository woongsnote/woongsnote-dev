---
import Timer from '@icons/timer.svg';

type Props = {
  readingTime: number;
};

const { readingTime } = Astro.props;
---

<span class="flex w-fit items-center gap-1">
  <Timer class="inline-block size-6" />
  {`읽는 데 ${readingTime}분`}
</span>
