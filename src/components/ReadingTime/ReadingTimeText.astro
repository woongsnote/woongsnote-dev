---
import { Icon } from 'astro-icon/components';

type Props = {
  readingTime: number;
};

const { readingTime } = Astro.props;
---

<span class="flex w-fit items-center gap-1 text-content">
  <Icon name="timer" class="inline-block size-6" />
  {`읽는 데 ${readingTime}분`}
</span>
